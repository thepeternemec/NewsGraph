{
  "name": "pleiades-worker",
  "version": "0.2.0",
  "description": "Pleiades ingestion worker: newsapi.ai → dedupe → packs (Phase 1).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@pleiades/contracts": "0.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
